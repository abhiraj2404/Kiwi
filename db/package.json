{
  "name": "@x402/db",
  "version": "1.0.0",
  "description": "Shared database package with Prisma client for X402 streaming payments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf dist",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "push": "prisma db push",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.6.0",
    "typescript": "^5.0.0",
    "tsx": "^4.7.0"
  },
  "files": [
    "dist",
    "prisma/schema.prisma",
    "README.md"
  ]
}

